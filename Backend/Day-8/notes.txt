video -> 20 mint done just code directly


1. response alwayse send in json formate's
2. status code -> just go on mdn status code for better understanding 


how can we wite in code here we have a example 

    app.get("/", async (req, res) => {
  try {
  } catch (err) {
    console.log(err);
  }
  res.status(500).send("welcome");
});